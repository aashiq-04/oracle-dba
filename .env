# ============================================================================
# Oracle DBA Platform - Environment Configuration
# ============================================================================

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
SERVER_READ_TIMEOUT=15s
SERVER_WRITE_TIMEOUT=15s
SERVER_SHUTDOWN_TIMEOUT=10s

# PostgreSQL Configuration (Platform Database)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Mohd@oct04
POSTGRES_DB=oracle_dba_platform
POSTGRES_SSLMODE=disable
POSTGRES_MAX_CONNS=25
POSTGRES_MIN_CONNS=5

# Oracle Configuration (Target Database to Monitor)
ORACLE_HOST=127.0.0.1
ORACLE_PORT=1521
ORACLE_SERVICE_NAME=xepdb1
ORACLE_USERNAME=oracle_monitor_pdb
ORACLE_PASSWORD=oracle_monitor_123
ORACLE_MAX_CONNS=10
ORACLE_MIN_CONNS=2

# JWT Configuration
JWT_SECRET=2a5bdd52e4813ef1c48b4018de5c05bc819790a3d7e8884426554ca949ae488c
JWT_EXPIRATION=24h
JWT_ISSUER=oracle-dba-platform

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=json

# ============================================================================
# IMPORTANT SECURITY NOTES:
# 1. Never commit this file to version control with real passwords
# 2. Change JWT_SECRET to a truly random string (use: openssl rand -base64 32)
# 3. Use strong passwords for PostgreSQL and Oracle
# 4. In production, use secrets management (Vault, AWS Secrets Manager, etc.)
# ============================================================================